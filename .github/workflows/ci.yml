name: Rom-Builder

on:
  push:
    branches: [ main ]

env:
  tg_token: ${{ secrets.TG_TOKEN }}
  tg_id: ${{ secrets.TG_CHAT_ID }}

jobs:
  build:
    name: Rom-Builder
    runs-on: ubuntu-latest

    steps:
    - name: Clone repo & checkout code
      uses: actions/checkout@v2

    - name: Notif Start
      run: |
        curl -s -X POST "https://api.telegram.org/bot${tg_token}/sendMessage" -d chat_id="${tg_id}" -d "disable_web_page_preview=true" -d "parse_mode=html" -d text="===================================%0A        🔱<b>Rom-Builder tool started</b>🔱%0A==================================="
        printenv

############################################################

  deploy-juice:
    name: POCO M3/9T
    needs: build
    runs-on: ubuntu-latest
    if: github.actor == 'c3eru'
    timeout-minutes: 360
    steps:
    - name: Clone Script..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        args: "-tt"
        command: |
          my_dir="$HOME"
          mkdir -p $my_dir/script
          cd $my_dir/script
          export device="juice"
          rm -rf *
          wget https://raw.githubusercontent.com/NFS-projects/Rom-Builder/main/script/sync.sh
          wget https://raw.githubusercontent.com/NFS-projects/Rom-Builder/main/script/building.sh
          wget https://raw.githubusercontent.com/NFS-projects/Rom-Builder/main/script/upload.sh
          wget https://raw.githubusercontent.com/NFS-projects/Rom-Builder/main/script/notif.sh
          wget https://raw.githubusercontent.com/NFS-projects/Rom-Builder/main/$device/build_rom.sh

    - name: Sync Rom..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        args: "-tt"
        command: |
          bash $HOME/script/sync.sh

    - name: Building Rom..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        args: "-tt"
        command: |
          bash $HOME/script/building.sh

    - name: Upload Rom..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        command: |
          bash $HOME/script/upload.sh

    - name: Send notif..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        args: "-tt"
        command: |
          bash $HOME/script/notif.sh

############################################################

  deploy-rosy:
    name: Redmi 5
    needs: build
    runs-on: ubuntu-latest
    if: github.actor == 'NFS86'
    timeout-minutes: 360
    steps:
    - name: Clone Script..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        args: "-tt"
        command: |
          my_dir="$HOME"
          mkdir -p $my_dir/script
          cd $my_dir/script
          export device="rosy"
          rm -rf *
          wget https://raw.githubusercontent.com/NFS-projects/Rom-Builder/main/script/sync.sh
          wget https://raw.githubusercontent.com/NFS-projects/Rom-Builder/main/script/building.sh
          wget https://raw.githubusercontent.com/NFS-projects/Rom-Builder/main/script/upload.sh
          wget https://raw.githubusercontent.com/NFS-projects/Rom-Builder/main/script/notif.sh
          wget https://raw.githubusercontent.com/NFS-projects/Rom-Builder/main/$device/build_rom.sh

    - name: Sync Rom..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        args: "-tt"
        command: |
          bash $HOME/script/sync.sh

    - name: Building Rom..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        args: "-tt"
        command: |
          bash $HOME/script/building.sh

    - name: Upload Rom..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        command: |
          bash $HOME/script/upload.sh

    - name: Send notif..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        args: "-tt"
        command: |
          bash $HOME/script/notif.sh

############################################################

  nonbuild:
    name: Comming son
    runs-on: ubuntu-latest

    steps:
    - name: Clone repo & checkout code
      uses: actions/checkout@v2

    - name: Notif Start
      run: |
        echo Hello...

  deploy-whyred:
    name: Redmi note 5 pro
    needs: nonbuild
    runs-on: ubuntu-latest
    steps:
    - name: Clone repo & checkout code
      uses: actions/checkout@v2

    - name: Test
      run: |
        echo Hello...

  deploy-tulip:
    name: Redmi note 6 pro
    needs: nonbuild
    runs-on: ubuntu-latest
    steps:
    - name: Clone repo & checkout code
      uses: actions/checkout@v2

    - name: Test
      run: |
        echo Hello...

  deploy-ginkgo:
    name: Redmi note 8 pro
    needs: nonbuild
    runs-on: ubuntu-latest
    steps:
    - name: Clone repo & checkout code
      uses: actions/checkout@v2

    - name: Test
      run: |
        echo Hello...

############################################################

  deploy-disk:
    name: Check Disk
    runs-on: ubuntu-latest
    steps:
    - name: Check Disk Info..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        args: "-tt"
        command: |
          echo #############################
          df -h
          lsblk
          echo #############################

  deploy-user:
    name: Check User active
    runs-on: ubuntu-latest
    steps:
    - name: Check User Info..
      uses: NFS86/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        user: medusa
        pass: ${{ secrets.PASSWORD }}
        args: "-tt"
        command: |
          echo #############################
          w
          echo #############################
